version: "3.8"

services:
  docs:
    build: .
    container_name: neunuc-docs
    ports:
      - "127.0.0.1:8080:80"
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /var/cache/nginx
      - /var/run
    networks:
      - docs-net

networks:
  docs-net:
    driver: bridge
